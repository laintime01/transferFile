Transfer files via socket in Python  

knowledge:  
response =b"Hello World!" means this a bytes object  
hint:server and browser only recognise bytes data, Send() and Recv() only return bytes type  
